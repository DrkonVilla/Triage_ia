# .streamlit/secrets.toml
# ⚠️ NO subir este archivo a GitHub - Contiene credenciales sensibles

[postgres]
host     = "aws-0-us-east-1.pooler.supabase.com"
port     = 5432
database = "postgres"
user     = "postgres.qwenqyhrbwhcdmrqkhzg"
password = "123"

# URL del backend FastAPI (cambiar cuando deployes el backend)
API_BASE_URL = "http://localhost:8000"

# Credenciales de prueba (para mostrar en el login)
[default_credentials]
enfermera_user = "enfermera1"
enfermera_pass = "password123"
medico_user = "medico1"
medico_pass = "password123"
